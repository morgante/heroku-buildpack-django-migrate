#!/usr/bin/env bash

BUILD_DIR=$1

cat $ENV_DIR/BACKEND_DIR
BACKEND_DIR=$(cat $ENV_DIR/$BACKEND_DIR)

# exit early if app is not Django
if [ ! -f $BUILD_DIR/requirements.txt ] && [ ! -f $ENV_DIR/$BACKEND_DIR/manage.py ]; then
    exit 1
fi

echo "Django"
